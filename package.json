{
  "name": "fanduel-pace-totals",
  "version": "1.0.0",
  "private": true,
  "description": "FanDuel companion for live basketball totals using pace and shot volume",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "build": "node --check server.js && node --check src/routes.js && node --test test/*.test.js",
    "probe": "node scripts/probe-espn.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
